# dashboard 项目依赖清单
#
# 说明：
# - CLI 核心功能（log_reader/obsidian_reader/git_reader/report_generator）只用 Python 标准库
# - Web 应用（web_app.py）需要 Flask、gunicorn、markdown
# - 图表使用 Chart.js（前端 JS 库，已放在 static/chart.min.js），不是 Python 依赖

flask>=3.0
gunicorn>=21.0
markdown>=3.5