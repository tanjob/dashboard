# dashboard 项目依赖清单
#
# 说明：
# - 当前项目核心功能只用 Python 标准库（json/os/sys/datetime/subprocess），无需安装任何第三方库
# - 图表功能使用 Chart.js（前端 JS 库，已下载到 static/chart.min.js），不是 Python 库
# - pyecharts 为可选依赖：如果后续想用 Python 生成图表（替代 Chart.js），取消下面一行的注释并安装

# pyecharts>=2.0.0